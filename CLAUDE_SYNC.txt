SYSTEM-DIALOG FÜR CLAUDE.TERMINAL
==================================
Kopiere den folgenden Block und übergib ihn an claude.terminal, um den Systemkontext in die LICHTREICH-Umgebung (RAG/Orchestra/n8n) zu synchronisieren.

--- BEGIN CLAUDE PROMPT ---

Hallo Claude,

hier ist der Rückball aus dem AI Studio Frontend (Pass #3). Wir weiten den Loop jetzt strukturiert aus. Das Ziel ist es, den "Teppich zum Fliegen zu bringen" – über sämtliche Repos und Apps hinweg, maximal token-sparend (AI Studio = Frontend/UI/React, Terminal = Architektur/Verdrahtung/Backend, Groq/lokal = Textmasse).

Das haben wir im AI Studio (Public Landingpage Repo) erreicht:
1. Die Landingpage ist visuell (Bauhaus-CI) fertig: Hero (mit Waitlist-Toast), Betriebsketten-Karten (BOB-Briefweg etc.), StatusMatrix (IST->SOLL), HeadOfBoard (mit API Fetch & Whiteboard), Connectors-Dummy, Personas, GTM-Funnel, FAQ, Impressum, Datenschutz.
2. `TODO.md` wurde strategisch aktualisiert:
   - **Rollentrennung** (Public / Admin / System / User) ist nun harte Vorgabe.
   - **Asynchrone Verarbeitung**: Im `briefkasten` blockiert kein LLM das Frontend. Dokument -> n8n (BOB-Briefweg) -> Akte.
   - **Multi-Repo-Sync**: Dieser Ping-Pong-Loop ist der Standard für alle Subdomains.

Deine nächsten Aufgaben (Architektur & Verdrahtung):
1. **Multi-Repo Struktur**: Skizziere kurz die Architektur-Matrix für die Code-Verwaltung. Welche Repos (GitHub) entsprechen welchen Subdomains (z.B. `briefkasten`, `setup`, `board`)? Wie routen wir die Dokumente/Specs zwischen ihnen?
2. **Briefkasten-Asynchronität**: Liefere die JSON-Spezifikation (Payload-Struktur), wie das Frontend (`briefkasten.lichtreich.info`) ein hochgeladenes Foto an den n8n-Webhook (BOB-Briefweg) feuern soll, ohne auf die Antwort zu warten.
3. **Skill-Dokumentation**: Schreibe eine `LIESMICH-loop-agent.md`, die diesen Sync-Prozess (Ping-Pong zwischen AI Studio und Claude Terminal) als Meta-Prompt festhält. Diese Datei können wir dann in jedem Repo als `AGENTS.md` hinterlegen, damit die KI "without head" genau weiß, wie sie sich in das große Ganze einfügt.
4. **Nächste ZauberSPEC**: Erstelle die nächste `studioAI.zauberSPEC.md` für die App `briefkasten`. Wir bauen als Nächstes das User-Frontend (Upload-Maske, asynchroner Status-Indikator).

Bitte bestätige mir diesen Pass in einem `_SYNC-REPORT.md` und liefere die genannten Dokumente zurück.

--- END CLAUDE PROMPT ---
